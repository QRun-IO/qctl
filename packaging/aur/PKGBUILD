# Maintainer: QRun-IO <contact@qrun.io>
pkgname=qctl-bin
pkgver=0.2.1
pkgrel=1
pkgdesc="CLI for the QRun ecosystem - scaffold, package, and deploy QQQ apps"
arch=('x86_64')
url="https://github.com/QRun-IO/qctl"
license=('AGPL-3.0')
provides=('qctl')
conflicts=('qctl')
source_x86_64=("https://github.com/QRun-IO/qctl/releases/download/v${pkgver}/qctl-linux-amd64")
sha256sums_x86_64=('79654f35570d77d8e65b4856238e83df290f4ab196a2ec39099fa823a545d7f2')

package() {
    install -Dm755 "${srcdir}/qctl-linux-amd64" "${pkgdir}/usr/bin/qctl"
}
